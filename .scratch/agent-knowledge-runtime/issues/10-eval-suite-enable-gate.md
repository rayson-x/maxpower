# 10 — Eval 套件与知识工具上线门

**What to build:** 用 ScriptedLLMProvider 建立 CI 确定性 eval 套件：≥30 人工编写用例，覆盖工具选择（问动作/规则必须调对应工具）、拒答边界（库外数值拒答、领域外转介）、禁止声称（逐条）三类，每类含正反用例。门槛：禁止声称与领域拒答 100% 通过、工具选择零失误。门槛达标后翻转 06 的禁用 flag，知识工具正式启用。

**Blocked by:** 06. 知识检索双工具（默认禁用）; 07. 分级拒答与领域边界; 08. 行为改变对话准则注入; 09. 禁止声称输出过滤器

**Status:** ready-for-agent

- [ ] ≥30 用例在 CI 确定性通过（ScriptedLLMProvider，不依赖真实模型）
- [ ] 三类用例各含正反；边界案例（交叉话题、近义误伤）有用例
- [ ] 门槛达标后 06 flag 翻转，工具出现在 provider manifest
- [ ] 门槛未达标时 flag 保持关闭的测试
