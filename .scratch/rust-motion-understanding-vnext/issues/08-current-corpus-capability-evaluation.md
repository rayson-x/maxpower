# 08 — Current corpus capability and recognition evaluation

**What to build:** Freeze and evaluate the completed Rust motion-understanding engine across the current governed personal corpus, publishing exact action×view capability, measurable Rep performance, set-report coverage, trace completeness and evidence limits without turning unlabelled quality outputs into accuracy claims.

**Blocked by:** 03 — Current rigid-bar action-family reuse; 04 — Cable and moving-handle fixed-equipment reuse; 05 — Constrained machine-lever reuse; 06 — Dual-dumbbell lateral-raise reuse; 07 — Body-only and fixed-support reuse.

**Status:** completed

- [x] Evaluation selects every consumed source and field by governed asset ID/admission and records immutable source identity, supervision authority, source-group key and hash where available.
- [x] All 54 current records resolve exact context and Bundle or publish a typed unsupported/refusal reason.
- [x] The report is segmented by exact action×view and action family rather than averaging incompatible contexts.
- [x] Human-supported metrics include context resolution and the admitted frozen output's Rep precision/recall and start/end boundary error; reviewed-negative-window false-trigger rate is explicitly unavailable because no admitted frozen output currently carries it.
- [x] Structural metrics include local-coordinate state, pose/equipment fact availability, fusion state, non-empty set-report generation, per-dimension judgeability, reference availability, typed refusal distribution and trace completeness.
- [x] Phase endpoints, equipment trajectories and quality judgements are reported only as availability/refusal because current governed data has no admitted human truth for their accuracy.
- [x] Touched owner regression is clearly separated from source-independent generalization; no current result claims cross-user population accuracy.
- [x] The capability matrix distinguishes executable context support from per-record Rep failures; every supported context has real Rep output, a non-empty set assessment and complete trace in governed replay, while three failing records remain explicit gaps.
- [x] The report identifies the smallest missing evidence or implementation gap for every unsupported current context and provides the starting map for later action-family Bundle expansion.
